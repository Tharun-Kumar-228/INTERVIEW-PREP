bool isSubsequence(char* s, char* t) {
    size_t n = strlen(s);
    size_t m = strlen(t);

    size_t i = 0;       
    size_t j = 0;          

    while (i < n && j < m) {
        if (s[i] == t[j])   
            ++i;
        ++j;               
    }
    return i == n;  
}