void rotate(int* nums, int n, int k) {
    // int arr[k];
    // nums[numsSize-k];
    // int st=(numsSize/k)+1;
    // int i=0;
    // for(int i=st;i<k;i++)
    // {
    //         arr[i]=nums[k];
    //         k++;
    // }
    // for(int i=0;i<numsSize;i++)
    // {
    //     nums[i]=nums[i]+arr[i];
    //     printf("%d",nums[i]);
    // }
    int temp[n];
    for(int i=0;i<n;i++)
        temp[(i+k)%n] = nums[i];
    for(int i=0;i<n;i++)
        nums[i]= temp[i];
}
