int kItemsWithMaximumSum(int numOnes, int numZeros, int numNegOnes, int k) {
    int sum = 0;
    int One = (k < numOnes) ? k : numOnes;
    sum += One;
    k -= One;
    int Zero = (k < numZeros) ? k : numZeros;
    k -= Zero;
    sum -= k; 

    return sum;
}